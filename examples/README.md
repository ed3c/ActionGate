# Examples

The first convergence canary is a non-financial protected DevOps action owned by `X01` / Issue #11. Examples cannot bypass the same protected-tool gateway or substitute mock evidence for physical-device claims.