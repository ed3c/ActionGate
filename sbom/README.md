# SPDX SBOM

An exact-subject SPDX SBOM has not yet been generated because no product upstream or source import is admitted and the implementation dependency graph is not frozen.

A fabricated or empty “release SBOM” would be misleading. The release gate requires a generated SPDX document bound to the exact candidate SHA and tree, admitted upstream lock, imported-path map, dependency manifests and preserved notices.

Current state: `SBOM_PENDING_EXACT_SOURCE_GRAPH`.
